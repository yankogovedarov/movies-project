# Итерации — правила и план

## Workflow правила

| Параметър | Избор |
|-----------|-------|
| Стил | **TDD-strict slices** — всяка итерация започва с failing test, после имплементация |
| Test policy | **TDD (test-first)** |
| Размер на итерация | **Малки (1-2 часа)** |
| Планиране | В plan mode преди всяка итерация — изпълнение само след одобрение |

### Правила за всяка итерация

1. **Преди старт** — plan mode: дефинираме failing tests + implementation scope
2. **Изпълнение ВИНАГИ:**
   - Failing test(ове) първо (red)
   - Минимална имплементация → green
   - Refactor ако е нужен
   - Верификация: `go test ./...`, `go build`, ръчен тест ако е приложимо
   - Commit: `Итерация NN: <feature>`
3. **Vertical slicing** — всяка итерация дава работещ end-to-end функционал
4. **Без scope creep** — извън обхвата → записва се за бъдеща итерация
5. **Детайлният план** на всяка итерация се пази в отделен файл `Итерация NN - <name>.md`

---

## Планирани итерации

| # | Заглавие | Кратко описание | Статус |
|---|----------|-----------------|--------|
| 01 | Walking skeleton | gin + hello world, пълен dev workflow | ✅ завършена |
| 02 | Disk discovery | `os.Executable()`-based path detection | ✅ завършена |
| 03 | Basic file scanner | Рекурсивно намиране на `.mkv/.avi/.mp4` файлове | ✅ завършена |
| 04 | Sample filtering | Skip < 50 MB видео когато има по-голям sibling | ✅ завършена |
| 05 | Excluded folders | Hardcoded exclude list в scanner | ✅ завършена |
| 06 | SQLite + migrations + sqlc | DB setup, първа миграция, sqlc config | ✅ завършена |
| 07 | Persist scan results | Scanner → DB upsert + on_disk flag | ✅ завършена |
| 08 | Web list (templ + Pico.css) | `GET /` показва списък медии | ✅ завършена |
| 09 | VLC detection | Path detection + per-computer config | ✅ завършена |
| 10 | Start media endpoint | `POST /media/:id/start` → запис event + VLC | ✅ завършена |
| 11 | Status change | `POST /media/:id/status` + UI | ✅ завършена |
| 12 | Background scan + manual button | goroutine + `POST /scan` | ✅ завършена |
| 13 | MVP-1 polish | Error handling, logging (slog + lumberjack) | ✅ завършена |
| **MVP-1 готов** | | | |
| 14 | API request tests setup | goquery integration | ✅ завършена |
| 15 | E2E setup | Python Playwright + pyproject.toml + първи E2E тест | ✅ завършена |
| 16 | Tree view | Дървовиден изглед | ✅ завършена |
| 17 | Detail view | Детайлен изглед с история | ✅ завършена |
| 18 | Filters | По статус + по наличност | ✅ завършена |
| 19 | Sorting | Сортиране по 4 критерия + "Последно стартирана" в списъка | ✅ завършена |
| 20 | Sort by file date | Сортиране по дата на файла (file_created_at) | ✅ завършена |
| 21 | Бъг 12 | Сортиране по път/размер + посока asc/desc + случаен нов | ✅ завършена |
| 22 | Бъг 13 | Търсене по текст, тип превод, офлайн режим, icon бутони | ✅ завършена |
| **MVP-2 готов — v1.0.0 в продукшън** | | | |

> **Бранч:** v1.0.0 (MVP-1 + MVP-2) е в продукшън на `master`. Нови итерации се разработват в бранч `develop` и се сливат в `master` при релийс. Виж [Архитектура.md §9.1](../Архитектура.md).

### Очакван обхват

**MVP-1:** ~13 итерации × 1-2 часа = ~15-25 часа работа
**MVP-2:** ~9 итерации × 1-2 часа = ~10-18 часа работа
