# Итерация 32 — Бъг 18 (2): правописна грешка „филтър" + версия

## Цел

Надписът за групата филтри в горната лента е изписан с грешка: **„філтър:"** —
вторият символ е украинското „і" (U+0456) вместо българското „и". Трябва да е
**„филтър:"**.

Освен това, с тази итерация Бъг 18 е напълно завършен → вдигаме версията на
приложението на **v1.3.0**.

## Обхват

- `templates/list.templ` (+ регенериран `list_templ.go`) — корекция на надписа.
- `Taskfile.yml` — `VERSION: v1.2.0` → `v1.3.0`.
- `docs/Открити бъгове.md` — секция „Реализирано" за Бъг 18 + статус + ред в
  обобщаващата таблица.

## TDD

**Red → Green:**

- Нов goquery тест `TestIndex_FilterLabel_Spelling` в `handlers_goquery_test.go`:
  - тялото на страницата съдържа правилния надпис „филтър:";
  - тялото **не** съдържа сгрешения „філтър:".

## Проверка

- `task test` (Go) — зелено.
- `task test:e2e` — зелено.

## Комит

`feat: Итерация 32 — Бъг 18 (2): правопис „филтър" + версия v1.3.0`
